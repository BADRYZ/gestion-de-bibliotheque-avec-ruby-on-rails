<style>
    .navbar-nav.navbar-center {
        position: absolute;
        left: 50%;
        transform: translatex(-50%);
    }
</style>

<!-- Votre vue -->
<nav class="navbar navbar-default">
    <div class="container-fluid">
        <!-- Brand and toggle get grouped for better mobile display -->
        <div class="navbar-header">
            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse"
                data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <a class="navbar-brand" id="logo" href="#">BIBLIO ENSAK</a>
        </div>

        <!-- Collect the nav links, forms, and other content for toggling -->
        <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
            <% if logged_in? %>
                <ul class="nav navbar-nav navbar-center">
                    <li><%= link_to "Adhérent", adherents_path %></li>
                    <li><%= link_to "Document", documents_path %></li>
                    <li><%= link_to "Materiel", materiels_path %></li>
                    <li><%= link_to "Emprunt", emprunts_path %></li>
                </ul>
                <%= link_to "Déconnexion", logout_path, method: :delete, class: "btn btn-danger navbar-btn navbar-right" %>
            <% else %>
                <%= link_to "Connexion", login_path, class: "btn btn-success navbar-btn navbar-right" %>
            <% end %>
        </div>
    </div>
</nav>
